import React, { useState } from 'react'
import '../App.css'

function Login({ }) {
    const [errors, setErrors] = useState([])

    function onSubmit(e){
        e.preventDefault()
        const customer = {
            username,
            password
        }
       
        
    }

        return (
            <div>
                
            </div>
    )
}

export default Login;